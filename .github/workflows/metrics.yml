name: Metrics

on:
  workflow_dispatch:

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@v3
        with:
          filename: metrics.svg
          token: ${{ secrets.TOKEN }}
          base: header
          output_action: commit